# Pinned dependencies for coder-harness-gateway
# Install: pip install -r requirements.txt
#
# Only the packages actually imported by scripts/ and tests/ are listed here.
# Use exact pins (==) to ensure reproducible, auditable installs.

boto3==1.35.36
botocore==1.35.36

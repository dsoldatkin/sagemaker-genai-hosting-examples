# Claude Code gateway environment example.
# Verify exact variable support against the installed Claude Code version.

export ANTHROPIC_BASE_URL="https://llm-gateway.example.com"
export ANTHROPIC_API_KEY="replace-with-gateway-key"

# Optional: use a gateway model alias if your Claude Code version supports model override.
export ANTHROPIC_MODEL="coding-default"
